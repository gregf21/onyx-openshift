
=======================================================================
  Onyx for OpenShift has been installed!
=======================================================================

{{- if .Values.vectorDB.enabled }}
MODE: Full (RAG + document connectors enabled)

Components deployed:
  - PostgreSQL (StatefulSet)
  - Redis (StatefulSet)
  - Vespa vector database (StatefulSet)
  - API server (Deployment)
  - Web server (Deployment)
  - Inference model server (Deployment)
  - Indexing model server (Deployment)
  - Celery beat scheduler (Deployment)
  - 7 Celery workers (Deployments)
  - Nginx reverse proxy (Deployment)
{{- else }}
MODE: Lite (LLM chat only — no RAG or document connectors)

Components deployed:
  - PostgreSQL (StatefulSet)
  - API server (Deployment)
  - Web server (Deployment)
  - Nginx reverse proxy (Deployment)
{{- end }}

Access Onyx (port-forward for testing):
  kubectl port-forward svc/{{ include "onyx.fullname" . }}-nginx {{ .Values.nginx.service.port }}:{{ .Values.nginx.service.port }} -n {{ .Release.Namespace }}
  Then open: http://localhost:{{ .Values.nginx.service.port }}

  For external access, ask your cluster admin to create a Route pointing
  to the {{ include "onyx.fullname" . }}-nginx service on port {{ .Values.nginx.service.port }}.
