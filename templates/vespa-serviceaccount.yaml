{{- if .Values.vectorDB.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "onyx.fullname" . }}-vespa
  labels:
    {{- include "onyx.labels" . | nindent 4 }}
    app.kubernetes.io/component: vespa
  annotations:
    # Grant anyuid SCC to this ServiceAccount with:
    #   oc adm policy add-scc-to-user anyuid -z {{ include "onyx.fullname" . }}-vespa -n <namespace>
{{- end }}
